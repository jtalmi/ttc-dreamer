---
phase: 08-station-first-drawing-model
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - components/editor/toronto-map.tsx
  - components/editor/editor-shell.tsx
autonomous: false
requirements:
  - DRAW-01
  - DRAW-02
  - DRAW-03

must_haves:
  truths:
    - "Clicking the map in draw mode places a station and the line auto-connects from the previous station"
    - "Clicking on an existing line segment in draw mode inserts a new station at that point"
    - "Clicking on a line terminus starts an extension from that endpoint"
    - "Double-click places final station AND finishes drawing"
    - "Ghost/preview line extends from last placed station position to cursor"
    - "Undo during session removes the last placed station via undoPlaceStation"
    - "Clicking empty map with no active session auto-creates a new line and places first station"
  artifacts:
    - path: "components/editor/toronto-map.tsx"
      provides: "Station-first click handlers for draw-line mode"
      contains: "placeStation"
    - path: "components/editor/editor-shell.tsx"
      provides: "Updated drawing flow wiring with station-first model"
      contains: "undoPlaceStation"
  key_links:
    - from: "components/editor/toronto-map.tsx"
      to: "lib/proposal/proposal-state.ts"
      via: "dispatch placeStation action on map click"
      pattern: "type.*placeStation"
    - from: "components/editor/toronto-map.tsx"
      to: "lib/proposal/proposal-geometry.ts"
      via: "buildInProgressGeoJSON with draft parameter"
      pattern: "buildInProgressGeoJSON.*draft"
    - from: "components/editor/editor-shell.tsx"
      to: "lib/proposal/proposal-state.ts"
      via: "dispatch undoPlaceStation for undo during drawing"
      pattern: "undoPlaceStation"
---

<objective>
Wire the station-first drawing model into the map click handlers and editor shell, replacing the old waypoint-based drawing interaction with station placement.

Purpose: This connects the rewritten data model (Plan 01) to the user-facing map interactions. After this plan, clicking the map in draw mode places stations and lines auto-connect between them.

Output: Updated toronto-map.tsx with station-first click handlers, updated editor-shell.tsx with new drawing flow and undo integration.
</objective>

<execution_context>
@/Users/jonathan/dev/ttc-dreamer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/dev/ttc-dreamer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-station-first-drawing-model/08-CONTEXT.md
@.planning/phases/08-station-first-drawing-model/08-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that the executor needs. -->

From lib/proposal/proposal-types.ts (after Plan 01):
```typescript
export type DrawingSession = {
  lineId: string;
  placedStationIds: string[];
  cursorPosition: [number, number] | null;
  mode: "new" | "extend" | "branch";
};
```

From lib/proposal/proposal-state.ts (after Plan 01):
```typescript
// New action type:
type UndoPlaceStationAction = { type: "undoPlaceStation" };

// placeStation now also adds to drawingSession.placedStationIds when session is active
// finishDrawing now derives waypoints from station positions
// cancelDrawing now removes session stations from draft
// addWaypoint action REMOVED
// startDrawing payload may include initialStationId for extend mode
```

From lib/proposal/proposal-geometry.ts (after Plan 01):
```typescript
export function deriveWaypointsFromStations(
  stationIds: string[],
  stations: ProposalStationDraft[]
): [number, number][];

// buildInProgressGeoJSON now takes (session, draft, lineColor) instead of (session, lineColor)
export function buildInProgressGeoJSON(
  session: DrawingSession | null,
  draft: ProposalDraft,
  lineColor: string,
): FeatureCollection | null;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite toronto-map.tsx click handlers and editor-shell.tsx drawing flow for station-first model</name>
  <files>
    components/editor/toronto-map.tsx,
    components/editor/editor-shell.tsx
  </files>
  <read_first>
    components/editor/toronto-map.tsx,
    components/editor/editor-shell.tsx,
    lib/proposal/proposal-types.ts,
    lib/proposal/proposal-state.ts,
    lib/proposal/proposal-geometry.ts,
    lib/proposal/index.ts
  </read_first>
  <action>
    **toronto-map.tsx changes:**

    1. **Remove onAddWaypoint prop** — it is no longer needed. Drawing clicks dispatch placeStation directly via dispatch.
    2. **Update buildInProgressGeoJSON call** (the useMemo): pass `(drawingSession, draft, activeLineColor)` instead of `(drawingSession, activeLineColor)` to match the new 3-argument signature from Plan 01.
    3. **Rewrite handleClick for draw-line mode:**
       - **If a drawing session IS active:**
         a. Generate `stationId = crypto.randomUUID()` and `stationName = "Station N"` (where N = draft.stations.length + 1).
         b. Snap the click position: first check if click is near an existing proposal line segment via `snapToSegment` — if so, use snapped position (this handles DRAW-02: mid-line station insertion). If not, use the raw lngLat.
         c. Check for nearby stations via `findNearbyStation` — if found, dispatch `suggestInterchange` (same flow as current add-station tool). If not found, dispatch `placeStation` with `{ id: stationId, position: snappedOrRawPosition, lineId: session.lineId, name: stationName }`. Show the StationNamePopover via setPendingStationName.
       - **If NO drawing session is active:**
         a. Check if click is near a proposal line terminus: iterate draft.lines, for each line with stationIds.length > 0, get the first and last station positions from draft.stations, check screen distance. If near a terminus, dispatch `startDrawing` with `{ lineId: line.id, mode: "extend", initialStationId: terminusStationId }` to continue drawing from that station. This handles DRAW-03 for proposal lines.
         b. Check if click is near a TTC baseline line (existing detectLineHitType flow for extend/branch). If hit, call `onStartExtend` (same as current).
         c. If no line terminus or TTC line hit: **auto-create a new line** and place the first station. Generate a new line ID and station ID. Dispatch `addLine` with default name ("Line N") and next default color. Then dispatch `startDrawing` with `{ lineId: newLineId, mode: "new" }`. Then dispatch `placeStation` with the click position on the new line. This handles the frictionless "click empty map" flow per CONTEXT.md decisions.
    4. **Rewrite handleDblClick:** In addition to preventing default zoom, dispatch `placeStation` for the double-click position (so the final click also places a station), then dispatch `finishDrawing`. This handles the "double-click places final station AND finishes drawing" requirement.
    5. **Update handleMouseMove for draw-line mode:** keep the cursor update (`onUpdateCursor` replaced by dispatching `updateCursorPosition` directly via dispatch). Compute snap target using findSnapTarget (existing logic is fine).
    6. **Remove the onAddWaypoint callback usage** from the handleClick function and the props type. Remove it from TorontoMapProps interface.
    7. **Cursor style:** draw-line mode should show "crosshair" (already correct).
    8. **Add-station tool click handler:** This tool now becomes a "click on existing line to insert station mid-line" tool. Keep the existing snapToSegment + placeStation flow but make sure it also works within the station-first model. Since placeStation already updates line.stationIds, the mid-line insertion should work — but the station needs to be inserted at the correct position in stationIds (between the adjacent stations of the segment). Dispatch a new `insertStationOnLine` action if needed, or adjust the placeStation handler to detect segment position. Implementation choice: use `snapToSegment` to get the segmentIndex, then dispatch placeStation with an additional `insertAfterIndex` payload field OR handle ordering in the reducer. For simplicity, add an optional `insertAtIndex` to PlaceStationAction payload that, when provided, splices the station ID into line.stationIds at that position instead of appending.

    **editor-shell.tsx changes:**

    1. **Remove onAddWaypoint prop** from the TorontoMap JSX — it is no longer passed.
    2. **Update the drawing status panel text:** Change "Click to place waypoints. Double-click to finish." to "Click to place stations. Double-click to finish."
    3. **Update handleStartDrawing:** When creating a new line and starting drawing, dispatch `startDrawing` with `mode: "new"` (no initialStationId — same as current, already correct).
    4. **Update handleStartExtend:** When extending from a TTC line, the flow creates a new proposal line. The `startDrawing` dispatch should pass `initialStationId` if extending from a station that exists in draft.stations. For TTC extends, since the TTC station is a baseline station (not in draft.stations), `initialStationId` should be omitted — the first click will place the first station.
    5. **Update undo keyboard shortcut during drawing:** When a drawing session is active and Ctrl+Z is pressed, dispatch `undoPlaceStation` instead of the generic `undo`. If `session.placedStationIds.length === 0`, dispatch `cancelDrawing` instead.
       ```typescript
       if (mod && e.key === "z" && !e.shiftKey) {
         e.preventDefault();
         if (chrome.drawingSession) {
           if (chrome.drawingSession.placedStationIds.length > 0) {
             dispatch({ type: "undoPlaceStation" });
           } else {
             dispatch({ type: "cancelDrawing" });
           }
         } else {
           dispatch({ type: "undo" });
         }
         return;
       }
       ```
    6. **Finish button minimum stations:** The "Finish Line" button should be disabled when `drawingSession.placedStationIds.length < 2`.
    7. **Update the Finish Line button text to also reference station count:** e.g., show station count like "Finish Line (N stations)" in the drawing-status sidebar panel when session is active.
  </action>
  <verify>
    <automated>cd /Users/jonathan/dev/ttc-dreamer && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "placeStation" components/editor/toronto-map.tsx
    - grep -q "undoPlaceStation" components/editor/editor-shell.tsx
    - grep -q "placedStationIds" components/editor/editor-shell.tsx
    - grep -rL "onAddWaypoint" components/editor/toronto-map.tsx (should NOT contain onAddWaypoint)
    - grep -q "Click to place stations" components/editor/editor-shell.tsx
    - npm run typecheck exits 0
    - npm run lint exits 0
  </acceptance_criteria>
  <done>
    Map click in draw mode dispatches placeStation (not addWaypoint). Double-click places final station and finishes. Undo during drawing dispatches undoPlaceStation. Empty-map click auto-creates line. Terminus click starts extend. TypeScript and lint clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify station-first drawing model in browser</name>
  <files>components/editor/toronto-map.tsx, components/editor/editor-shell.tsx</files>
  <read_first>components/editor/toronto-map.tsx, components/editor/editor-shell.tsx</read_first>
  <action>
    Human verification of the station-first drawing model. Run `npm run dev` and open http://localhost:3000 to test all drawing interactions in the browser.
  </action>
  <what-built>Complete station-first drawing model: clicking the map in draw mode places stations with auto-connecting lines, double-click finishes, undo removes last station, terminus click extends, mid-line click inserts station.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. **New line from empty map (DRAW-01):**
       - Click the Draw tool in the floating toolbar
       - Click on an empty area of the map — a new line should be auto-created and a station placed at the click point
       - Click 2-3 more points — stations should appear at each click and lines should connect between consecutive stations
       - The ghost/preview line should extend from the last placed station to the cursor
       - Double-click to finish — the line should be committed with all placed stations
    3. **Mid-line station insertion (DRAW-02):**
       - Switch to the Add Station tool
       - Click on the line you just drew, somewhere between two existing stations
       - A new station should appear at the click point, splitting that line segment
    4. **Terminus extension (DRAW-03):**
       - Switch to Draw tool
       - Click near one end of your existing line (the last station)
       - Drawing should continue from that station (extend mode)
       - Place 2 more stations and double-click to finish
       - The extended stations should be appended to the original line
    5. **Undo during drawing:**
       - Start a new drawing session
       - Place 3 stations
       - Press Cmd/Ctrl+Z — the last station should be removed and the ghost line should snap back to the second station
       - Press Cmd/Ctrl+Z again — the second station removed, ghost line from first
    6. **Share link compatibility:**
       - If you have any old share links, try opening them — they should still work (v1 migration)
    7. **Existing features still work:**
       - Select mode: click a station or line to inspect
       - Station dragging in select mode still works
       - Undo/redo of non-drawing actions still works
       - Delete line/station still works
  </how-to-verify>
  <verify>
    <automated>cd /Users/jonathan/dev/ttc-dreamer && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - User confirms drawing flow works in browser
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>
    User has verified in the browser that station-first drawing, mid-line insertion, terminus extension, undo during drawing, and existing features all work correctly.
  </done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run typecheck` — zero type errors
2. `npm run lint` — no new lint warnings
3. `npm run dev` — app loads without console errors
4. Full drawing flow works: click-to-place-station, auto-connect, double-click finish
5. Mid-line insertion works
6. Terminus extension works
7. Undo during drawing works
8. Existing features (select, inspect, delete, undo/redo) unbroken
</verification>

<success_criteria>
- User can draw lines by clicking to place stations (DRAW-01)
- User can insert stations on existing lines (DRAW-02)
- User can extend lines from terminus (DRAW-03)
- Undo steps back through station placements one at a time
- All prior editing features continue to work
- No TypeScript errors, no lint warnings
</success_criteria>

<output>
After completion, create `.planning/phases/08-station-first-drawing-model/08-02-SUMMARY.md`
</output>
