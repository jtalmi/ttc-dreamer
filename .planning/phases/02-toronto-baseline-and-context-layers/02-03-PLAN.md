---
phase: 02-toronto-baseline-and-context-layers
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - public/data/bus-corridors.geojson
  - public/data/streetcar-corridors.geojson
  - components/map/corridor-layers.tsx
  - components/editor/toronto-map.tsx
  - components/editor/editor-shell.tsx
  - components/editor/editor-frame.tsx
  - components/editor/top-toolbar.tsx
  - lib/proposal/proposal-types.ts
  - lib/proposal/proposal-state.ts
  - lib/proposal/index.ts
autonomous: false
requirements: [MAP-04]

must_haves:
  truths:
    - "User sees a 'Bus + Streetcar Corridors' toggle button in the top toolbar"
    - "Toggle defaults to off on initial load"
    - "When toggled on, major bus and streetcar corridors appear on the map as thin dashed lines"
    - "Corridors are visually subordinate to TTC rapid transit lines (thinner, lower opacity, dashed)"
    - "When toggled off, corridors disappear instantly with no loading delay"
    - "The full map looks and feels distinctly Toronto-native"
  artifacts:
    - path: "components/map/corridor-layers.tsx"
      provides: "Bus and streetcar corridor Source+Layer components with visibility toggle"
      exports: ["CorridorLayers"]
    - path: "public/data/bus-corridors.geojson"
      provides: "Major TTC bus corridor line features"
    - path: "public/data/streetcar-corridors.geojson"
      provides: "TTC streetcar route line features"
    - path: "components/editor/top-toolbar.tsx"
      provides: "Updated toolbar with Bus + Streetcar Corridors toggle button"
  key_links:
    - from: "components/editor/top-toolbar.tsx"
      to: "components/editor/editor-shell.tsx"
      via: "onCorridorToggle callback dispatches toggleCorridors action"
      pattern: "onCorridorToggle|toggleCorridors"
    - from: "components/editor/editor-shell.tsx"
      to: "components/editor/toronto-map.tsx"
      via: "busCorridorVisible prop passed through EditorFrame -> TorontoMap"
      pattern: "busCorridorVisible"
    - from: "components/editor/toronto-map.tsx"
      to: "components/map/corridor-layers.tsx"
      via: "CorridorLayers receives visible prop controlling layout.visibility"
      pattern: "<CorridorLayers"
---

<objective>
Add bus and streetcar corridor overlay with a toolbar toggle, completing all four MAP requirements. Verify the full Toronto-native map experience with a human checkpoint.

Purpose: The corridor toggle (MAP-04) is the only user-interactive map layer control in Phase 2. It completes the phase and demonstrates that the layer system works. The checkpoint confirms the map reads as distinctly Toronto.

Output: Toggleable corridor layer, updated toolbar with toggle button, updated reducer state, and a verified Toronto-native map experience.
</objective>

<execution_context>
@/Users/jonathan/dev/ttc-dreamer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/dev/ttc-dreamer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-toronto-baseline-and-context-layers/02-RESEARCH.md
@.planning/phases/02-toronto-baseline-and-context-layers/02-UI-SPEC.md
@.planning/phases/02-toronto-baseline-and-context-layers/02-01-SUMMARY.md
@.planning/phases/02-toronto-baseline-and-context-layers/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 and Plan 02 -- components this plan extends -->

From components/editor/toronto-map.tsx (after Plan 02):
```typescript
// TorontoMap currently renders:
//   <Map>
//     <ContextLabels ... />
//     <GoLayers ... />
//     <TtcLayers ... />
//     <StationLabels ... />
//     {hoverStation && <Popup ... />}
//   </Map>
// Plan 03 adds CorridorLayers between ContextLabels and GoLayers.
// Plan 03 also adds a `busCorridorVisible` prop to control corridor visibility.
export default function TorontoMap(): JSX.Element;
```

From lib/proposal/proposal-types.ts (Phase 1):
```typescript
export type EditorChromeState = {
  activeTool: ToolMode;
  sidebarOpen: boolean;
  // Plan 03 adds: busCorridorVisible: boolean;
};
```

From lib/proposal/proposal-state.ts (Phase 1):
```typescript
export type EditorShellAction =
  | SetBaselineModeAction
  | ToggleSidebarAction
  | SetActiveToolAction
  | ResetShellStateAction;
  // Plan 03 adds: | ToggleCorridorsAction
```

From components/editor/top-toolbar.tsx (Phase 1):
```typescript
type TopToolbarProps = Readonly<{
  activeTool?: ToolName;
  baseline?: BaselineMode;
  onToolSelect?: (tool: ToolName) => void;
  onBaselineChange?: (mode: BaselineMode) => void;
  // Plan 03 adds: busCorridorVisible?: boolean;
  // Plan 03 adds: onCorridorToggle?: () => void;
}>;
```

From components/editor/editor-frame.tsx (Phase 1):
```typescript
type EditorFrameProps = Readonly<{
  ...existing props...
  // Plan 03 adds: busCorridorVisible?: boolean;
  // Plan 03 adds: onCorridorToggle?: () => void;
}>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create corridor data, layer component, and wire toggle through the full state chain</name>
  <files>
    public/data/bus-corridors.geojson
    public/data/streetcar-corridors.geojson
    components/map/corridor-layers.tsx
    components/editor/toronto-map.tsx
    components/editor/editor-shell.tsx
    components/editor/editor-frame.tsx
    components/editor/top-toolbar.tsx
    lib/proposal/proposal-types.ts
    lib/proposal/proposal-state.ts
    lib/proposal/index.ts
    lib/baseline/baseline-data.ts
    lib/baseline/index.ts
  </files>
  <action>
    **Step 1 -- Fetch/create corridor GeoJSON data:**

    Fetch streetcar corridors from City of Toronto ArcGIS:
    ```bash
    curl -s "https://gis.toronto.ca/arcgis/rest/services/cot_geospatial7/FeatureServer/12/query?where=1%3D1&outFields=*&f=geojson" \
      > public/data/streetcar-corridors.geojson
    ```
    If the curl fails, hand-author simplified LineString features for ~10 streetcar routes (501 Queen, 504 King, 505 Dundas, 506 Carlton, 510 Spadina, 511 Bathurst, 512 St Clair, etc.).

    For bus corridors, hand-author `public/data/bus-corridors.geojson` with ~10-15 major TTC bus corridors as simplified LineString features. Properties: `{ name: "...", route: "..." }`. Include high-ridership routes:
    - 29 Dufferin, 36 Finch West, 54 Lawrence East, 60 Steeles West
    - 72 Pape, 86 Scarborough, 32 Eglinton West, 34 Eglinton East
    - 52 Lawrence West, 96 Wilson, 25 Don Mills, 7 Bathurst
    Each route needs only 3-5 waypoints showing the general corridor.

    Add loader functions `loadBusCorridors()` and `loadStreetcarCorridors()` to `lib/baseline/baseline-data.ts`. Export from `lib/baseline/index.ts`.

    **Step 2 -- Create CorridorLayers component (`components/map/corridor-layers.tsx`):**
    A `"use client"` component with props: `busCorridors: FeatureCollection`, `streetcarCorridors: FeatureCollection`, `visible: boolean`. Renders:

    - Bus corridor lines: `type: "line"` layer (id: `"bus-corridors"`):
      - `layout.visibility: visible ? "visible" : "none"` (reactive toggle)
      - `line-color: "#E05A2A"` (--bus-corridor-surface), `line-width: 2`
      - `line-dasharray: [4, 4]`, `line-opacity: 0.60`

    - Streetcar corridor lines: `type: "line"` layer (id: `"streetcar-corridors"`):
      - `layout.visibility: visible ? "visible" : "none"`
      - `line-color: "#E05A2A"` (same color as bus), `line-width: 2`
      - `line-dasharray: [4, 4]`, `line-opacity: 0.60`

    Export as named export `CorridorLayers`.

    **Step 3 -- Add `busCorridorVisible` to the reducer state chain:**

    Modify `lib/proposal/proposal-types.ts`:
    - Add `busCorridorVisible: boolean` to `EditorChromeState`

    Modify `lib/proposal/proposal-state.ts`:
    - Add action type `ToggleCorridorsAction = { type: "toggleCorridors" }`
    - Add it to the `EditorShellAction` union
    - Add case in `proposalEditorReducer`: toggle `chrome.busCorridorVisible`
    - Update `createInitialProposalDraft` to set `busCorridorVisible: false` (off by default per UI-SPEC)

    Update `lib/proposal/index.ts` barrel export if needed (the action type is already re-exported as the union).

    **Step 4 -- Wire the toggle through EditorFrame and TopToolbar:**

    Modify `components/editor/top-toolbar.tsx`:
    - Add props: `busCorridorVisible?: boolean` and `onCorridorToggle?: () => void`
    - Add a toggle button AFTER the baseline toggle group (to the right, per UI-SPEC interaction contract) and BEFORE the "Start Proposal" CTA
    - Button label: `"Bus + Streetcar Corridors"` (exact copy from UI-SPEC)
    - When `off` (busCorridorVisible is false/undefined): `backgroundColor: "var(--shell-secondary)"`, white text, border same as baseline toggle buttons
    - When `on` (busCorridorVisible is true): `backgroundColor: "var(--shell-accent)"`, white text -- matches active tool pill pattern
    - `aria-pressed={busCorridorVisible}` for accessibility
    - `onClick={() => onCorridorToggle?.()}`

    Modify `components/editor/editor-frame.tsx`:
    - Add props: `busCorridorVisible?: boolean`, `onCorridorToggle?: () => void`
    - Pass both down to `<TopToolbar>`
    - Add internal state fallback: `const [internalCorridorVisible, setInternalCorridorVisible] = useState(false)` with the same controlled/uncontrolled pattern used for other props

    Modify `components/editor/editor-shell.tsx`:
    - Pass `busCorridorVisible={chrome.busCorridorVisible}` to EditorFrame
    - Pass `onCorridorToggle={() => dispatch({ type: "toggleCorridors" })}` to EditorFrame
    - Pass `busCorridorVisible={state.chrome.busCorridorVisible}` to the dynamically-imported TorontoMap component (add a prop to TorontoMap)

    **Step 5 -- Wire CorridorLayers into TorontoMap:**

    Modify `components/editor/toronto-map.tsx`:
    - Accept new prop: `busCorridorVisible?: boolean` (defaults to false)
    - Load bus corridor and streetcar corridor data in the existing useEffect
    - Import and render `<CorridorLayers>` with the `visible` prop driven by `busCorridorVisible`
    - Insert `<CorridorLayers>` between `<ContextLabels>` and `<GoLayers>` in the render order (per UI-SPEC stacking: streets -> corridors -> GO -> TTC -> labels)

    **Conventions:**
    - Preserve Phase 1 controlled/uncontrolled duality pattern in EditorFrame
    - `Readonly<{...}>` for all props
    - `"use client"` on all modified client components
    - 2-space indentation, double quotes, semicolons, trailing commas
  </action>
  <verify>
    - `npm run typecheck` passes
    - `npm run lint` passes
    - `npm run build` completes without errors
    - `npm run dev` -- clicking "Bus + Streetcar Corridors" button toggles corridor visibility
  </verify>
  <done>
    - "Bus + Streetcar Corridors" toggle button appears in the toolbar (MAP-04)
    - Toggle defaults to off on initial load
    - When on, bus and streetcar corridors render as thin dashed lines at 60% opacity
    - When off, corridors are hidden (layout.visibility = "none")
    - Corridors are visually subordinate to TTC rapid transit lines
    - Toggle is instant (no loading state, no animation delay per UI-SPEC)
    - Reducer state correctly tracks busCorridorVisible through the full chain
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Toronto-native map experience</name>
  <files>none</files>
  <action>
    Present the complete Phase 2 map for human verification. All four MAP requirements have been implemented:
    - MAP-01: TTC rapid transit baseline (Lines 1, 2, 4, 5 in brand colors, station dots)
    - MAP-02: GO Transit context layer (dashed green lines, non-interactive stations)
    - MAP-03: Toronto labels (neighbourhoods, landmarks, major streets, station names)
    - MAP-04: Toggleable bus/streetcar corridor layer

    The map should feel unmistakably Toronto-native per the product vision.

    Verification steps for the user:
    1. Ensure `.env.local` has a valid `NEXT_PUBLIC_MAPTILER_KEY` (get free key from https://cloud.maptiler.com)
    2. Run `npm run dev` and visit http://localhost:3000
    3. Verify the map loads centered on Toronto (you should see the city)
    4. Verify TTC lines are bold and distinct: yellow (Line 1), green (Line 2), purple (Line 4), orange (Line 5)
    5. Verify TTC station dots appear as white circles with colored borders
    6. Verify GO lines appear as dashed green lines, clearly lighter than TTC
    7. Verify neighbourhood labels (e.g. "LESLIEVILLE", "ANNEX") appear in uppercase
    8. Verify landmark labels (e.g. "CN Tower", "Union Station") appear on the map
    9. Verify TTC station names appear next to station dots
    10. Hover over a TTC station -- a tooltip should show the station name and line
    11. Click "Bus + Streetcar Corridors" in the toolbar -- dashed lines should appear
    12. Click the toggle again -- corridors should disappear
    13. Verify the overall feel: does this look like Toronto? Are transit lines the visual hero?
    14. Verify the Phase 1 shell (toolbar, sidebar, baseline toggle) still works correctly
    15. Run `npm run build` -- should complete without errors
  </action>
  <verify>User types "approved" or describes issues to fix</verify>
  <done>Human confirms the Phase 2 map looks and feels Toronto-native, all MAP requirements are met, and Phase 1 shell functionality is preserved</done>
</task>

</tasks>

<verification>
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- `npm run build` exits 0
- All corridor GeoJSON data files valid
- Toggle button present in toolbar with correct label
- Corridors show/hide on toggle
- Full map renders all four MAP requirements
</verification>

<success_criteria>
- Bus + Streetcar Corridors toggle appears in toolbar and works (MAP-04)
- Corridor lines are visually subordinate to rapid transit lines
- Toggle state flows correctly through reducer -> EditorFrame -> TorontoMap -> CorridorLayers
- Human confirms the full Phase 2 map looks and feels Toronto-native
- All Phase 1 shell functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/02-toronto-baseline-and-context-layers/02-03-SUMMARY.md`
</output>
