---
phase: 06-baseline-data-correction
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - public/data/ttc-stations-future.geojson
  - public/data/ttc-stations.geojson
  - components/map/ttc-layers.tsx
autonomous: true
requirements: [BASE-01, BASE-02, BASE-03]

must_haves:
  truths:
    - "Line 5 and Line 6 stations appear in both today and future station files with GTFS-accurate coordinates"
    - "Line 5 has 25 stations and Line 6 has 18 stations in the station files"
    - "Ontario Line has 15 stations in ttc-stations-future.geojson but not in ttc-stations.geojson"
    - "Ontario Line renders with a dashed/under-construction visual style on the map"
    - "Line 6 renders in grey #969594 instead of old construction grey #808080"
    - "TTC line paths visually run through their station dots with no large offset"
  artifacts:
    - path: "public/data/ttc-stations-future.geojson"
      provides: "Future station data with GTFS Line 5/6 coords + Ontario Line stations"
      contains: "Ontario Line"
    - path: "public/data/ttc-stations.geojson"
      provides: "Today station data with GTFS Line 5/6 coords added"
      contains: "ROUTE_ID"
    - path: "components/map/ttc-layers.tsx"
      provides: "Ontario Line dashed layer rendering and corrected Line 6 color"
      contains: "line-dasharray"
  key_links:
    - from: "public/data/ttc-stations-future.geojson"
      to: "components/map/ttc-layers.tsx"
      via: "GeoJSON source for station circle rendering"
      pattern: "ttc-stations"
    - from: "components/map/ttc-layers.tsx"
      to: "public/data/ttc-routes-future.geojson"
      via: "ROUTE_ID 7 filter for Ontario Line layer"
      pattern: "ROUTE_ID.*7"
---

<objective>
Replace inaccurate ArcGIS station coordinates for Lines 5 and 6 with GTFS-sourced data, add Lines 5/6 stations to the today baseline, add Ontario Line stations to the future baseline, and update ttc-layers.tsx to render the Ontario Line with a dashed construction style and correct the Line 6 color.

Purpose: Completes the baseline data correction so lines pass through station dots (BASE-01), Lines 5/6 show as operational and Ontario Line as under construction (BASE-02), and all current TTC rapid transit lines are represented (BASE-03).
Output: Corrected station GeoJSON files, updated TTC layer rendering component.
</objective>

<execution_context>
@/Users/jonathan/dev/ttc-dreamer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/dev/ttc-dreamer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-baseline-data-correction/06-RESEARCH.md
@.planning/phases/06-baseline-data-correction/06-01-SUMMARY.md

@public/data/ttc-stations.geojson
@public/data/ttc-stations-future.geojson
@components/map/ttc-layers.tsx
@lib/baseline/baseline-types.ts

<interfaces>
From lib/baseline/baseline-types.ts (updated in Plan 01):
export type TtcRouteProperties includes status?: "operational" | "under_construction"

From components/map/ttc-layers.tsx (current structure):
- TtcLayersProps = { routes: FeatureCollection; stations: FeatureCollection }
- Uses Source id="ttc-routes" with per-line Layer filters on ROUTE_ID
- Uses Source id="ttc-stations" with single circle layer id="ttc-stations-circle"

Subway station properties (no ROUTE_ID): { PT_NAME, ADDRESS_FULL, LONGITUDE, LATITUDE, ... }
LRT station properties (has ROUTE_ID): { PT_NAME, ROUTE_ID, TRANSIT_LINE, ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download TTC GTFS data and replace Line 5/6 station coordinates in both station files</name>
  <files>public/data/ttc-stations.geojson, public/data/ttc-stations-future.geojson</files>
  <read_first>
    - public/data/ttc-stations.geojson (68 features after Plan 01 -- subway stations only, no Lines 5/6)
    - public/data/ttc-stations-future.geojson (103 features -- 73 subway + 21 Line 5 + 9 Line 6 with bad ArcGIS coords)
    - .planning/phases/06-baseline-data-correction/06-RESEARCH.md (GTFS download URL, extraction pattern, station counts)
  </read_first>
  <action>
    **Step 1: Download and extract GTFS data**

    Download the TTC GTFS zip from:
    ```
    https://ckan0.cf.opendata.inter.prod-toronto.ca/dataset/7795b45e-e65a-4465-81fc-c36b9dfff169/resource/cfb6b2b8-6191-41e3-bda1-b175c51148cb/download/TTC%20Routes%20and%20Schedules%20Data.zip
    ```

    Extract to `/tmp/ttc_gtfs_data/`. Parse `stops.txt` to find Line 5 (Eglinton) and Line 6 (Finch West) stations. Cross-reference with `routes.txt`, `trips.txt`, and `stop_times.txt` to identify which stops belong to which route.

    For each LRT station, average the coordinates of all platform entries (westbound/eastbound) for the same base station name to get a single representative midpoint on or near the track centerline.

    **Step 2: Update ttc-stations-future.geojson**

    1. Remove all 21 existing Line 5 station features (those with ROUTE_ID: 5).
    2. Remove all 9 existing Line 6 station features (those with ROUTE_ID: 6).
    3. Add 25 new Line 5 station features using GTFS midpoint coordinates:
       ```json
       {
         "type": "Feature",
         "geometry": { "type": "Point", "coordinates": [lon, lat] },
         "properties": {
           "PT_NAME": "GTFS station name",
           "ROUTE_ID": 5,
           "TRANSIT_LINE": "Line 5 Eglinton",
           "status": "operational"
         }
       }
       ```
    4. Add 18 new Line 6 station features using GTFS midpoint coordinates (same schema, ROUTE_ID: 6, TRANSIT_LINE: "Line 6 Finch West").
    5. Add 15 Ontario Line station features with hand-authored coordinates:
       - Exhibition: [-79.4182, 43.6366]
       - King-Bathurst: [-79.4020, 43.6440]
       - Queen-Spadina: [-79.3932, 43.6485]
       - Osgoode: [-79.3867, 43.6508]
       - Queen: [-79.3810, 43.6530]
       - Moss Park: [-79.3735, 43.6545]
       - Corktown: [-79.3635, 43.6558]
       - East Harbour: [-79.3533, 43.6568]
       - Riverside-Leslieville: [-79.3425, 43.6618]
       - Gerrard: [-79.3370, 43.6680]
       - Pape: [-79.3384, 43.6780]
       - Cosburn: [-79.3395, 43.6870]
       - Thorncliffe Park: [-79.3430, 43.6990]
       - Flemingdon Park: [-79.3440, 43.7100]
       - Science Centre: [-79.3465, 43.7165]
       Each Ontario Line station: ROUTE_ID: 7, TRANSIT_LINE: "Ontario Line", status: "under_construction".

    Resulting file: 68 subway + 25 Line 5 + 18 Line 6 + 15 Ontario Line = 126 features.

    **Step 3: Update ttc-stations.geojson (today baseline)**

    Add the same 25 Line 5 and 18 Line 6 station features (with GTFS coords) to the today file. Do NOT add Ontario Line stations.

    Resulting file: 68 subway + 25 Line 5 + 18 Line 6 = 111 features.

    **Fallback:** If GTFS download fails, hand-author stations from Wikipedia station lists with approximate OSM coordinates. The key requirement is station dots within ~100m of route geometry.

    **Important:** Keep minimal property schema for new LRT stations (PT_NAME, ROUTE_ID, TRANSIT_LINE, status). Do NOT replicate all ArcGIS properties. Original 68 subway station features must remain exactly as they are.
  </action>
  <verify>
    <automated>python3 -c "
import json
today = json.load(open('public/data/ttc-stations.geojson'))
future = json.load(open('public/data/ttc-stations-future.geojson'))
t5 = [f for f in today['features'] if f['properties'].get('ROUTE_ID') == 5]
t6 = [f for f in today['features'] if f['properties'].get('ROUTE_ID') == 6]
t7 = [f for f in today['features'] if f['properties'].get('ROUTE_ID') == 7]
ts = [f for f in today['features'] if f['properties'].get('ROUTE_ID') is None]
f5 = [f for f in future['features'] if f['properties'].get('ROUTE_ID') == 5]
f6 = [f for f in future['features'] if f['properties'].get('ROUTE_ID') == 6]
f7 = [f for f in future['features'] if f['properties'].get('ROUTE_ID') == 7]
assert len(t5)==25 and len(t6)==18 and len(t7)==0 and len(ts)==68
assert len(today['features'])==111
assert len(f5)==25 and len(f6)==18 and len(f7)==15
assert len(future['features'])==126
print('All station checks passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - ttc-stations.geojson has exactly 111 features (68 subway + 25 Line 5 + 18 Line 6)
    - ttc-stations-future.geojson has exactly 126 features (68 subway + 25 Line 5 + 18 Line 6 + 15 Ontario Line)
    - Line 5 stations have ROUTE_ID 5 and GTFS-derived coordinates
    - Line 6 stations have ROUTE_ID 6 and GTFS-derived coordinates
    - Ontario Line stations have ROUTE_ID 7, status "under_construction", appear only in future file
    - All 68 original subway station features retain exact original ArcGIS properties
    - No Scarborough RT stations present in either file
  </acceptance_criteria>
  <done>Both station GeoJSON files contain accurate Line 5/6 coordinates from GTFS and Ontario Line stations in future baseline</done>
</task>

<task type="auto">
  <name>Task 2: Update ttc-layers.tsx with Ontario Line dashed rendering and Line 6 color fix</name>
  <files>components/map/ttc-layers.tsx</files>
  <read_first>
    - components/map/ttc-layers.tsx (current TTC layer rendering -- 5 route layers + 1 station circle layer)
    - .planning/phases/06-baseline-data-correction/06-RESEARCH.md (Ontario Line layer pattern, line-dasharray approach)
  </read_first>
  <action>
    Modify components/map/ttc-layers.tsx with these three changes:

    1. **Fix Line 6 color:** In the Layer with id="ttc-line-6", change the comment to "{/* Line 6 -- Finch West (grey) */}" and change "line-color" from "#808080" to "#969594".

    2. **Add Ontario Line base layer** inside the Source id="ttc-routes" block, after the Line 6 Layer:
       ```tsx
       {/* Ontario Line -- under construction (dashed blue) */}
       <Layer
         id="ttc-line-7-base"
         type="line"
         filter={["==", ["get", "ROUTE_ID"], 7]}
         paint={{
           "line-color": "#00A4E3",
           "line-width": 4,
           "line-opacity": 0.4,
         }}
         layout={{
           "line-cap": "round",
           "line-join": "round",
         }}
       />
       ```

    3. **Add Ontario Line dash overlay** immediately after the base layer:
       ```tsx
       <Layer
         id="ttc-line-7-dash"
         type="line"
         filter={["==", ["get", "ROUTE_ID"], 7]}
         paint={{
           "line-color": "#00A4E3",
           "line-width": 4,
           "line-dasharray": [4, 3],
         }}
         layout={{
           "line-cap": "round",
           "line-join": "round",
         }}
       />
       ```

    Do NOT change the station circle layer, Source structure, component props, imports, or export.
  </action>
  <verify>
    <automated>grep -q 'ttc-line-7-base' components/map/ttc-layers.tsx && grep -q 'ttc-line-7-dash' components/map/ttc-layers.tsx && grep -q 'line-dasharray' components/map/ttc-layers.tsx && grep -q '#969594' components/map/ttc-layers.tsx && grep -qv '#808080' components/map/ttc-layers.tsx && echo 'Layer checks passed' && cd /Users/jonathan/dev/ttc-dreamer && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ttc-layers.tsx contains Layer with id="ttc-line-7-base" with line-color "#00A4E3" and line-opacity 0.4
    - ttc-layers.tsx contains Layer with id="ttc-line-7-dash" with line-color "#00A4E3" and line-dasharray [4, 3]
    - ttc-layers.tsx contains "#969594" for Line 6 color (not "#808080")
    - No occurrence of "#808080" in ttc-layers.tsx
    - npm run typecheck passes with no errors
  </acceptance_criteria>
  <done>Ontario Line renders with dashed blue under-construction style, Line 6 shows correct operational grey color</done>
</task>

</tasks>

<verification>
- Station count check: `python3 -c "import json; t=json.load(open('public/data/ttc-stations.geojson')); f=json.load(open('public/data/ttc-stations-future.geojson')); print(f'today={len(t[\"features\"])}, future={len(f[\"features\"])}')"`  prints "today=111, future=126"
- Layer rendering check: `grep -c 'ttc-line-7' components/map/ttc-layers.tsx` returns 2 or more (base + dash layer IDs)
- Color fix check: `grep '#808080' components/map/ttc-layers.tsx` returns nothing (old grey removed)
- Type check: `npm run typecheck` passes
- Lint check: `npm run lint` passes
</verification>

<success_criteria>
Station GeoJSON files contain accurate GTFS-sourced coordinates for Lines 5 and 6, Ontario Line stations appear in the future baseline only, ttc-layers.tsx renders the Ontario Line with a visually distinct dashed construction style, and Line 6 displays its correct operational grey color.
</success_criteria>

<output>
After completion, create `.planning/phases/06-baseline-data-correction/06-02-SUMMARY.md`
</output>
