---
phase: 09-station-drag-auto-interchange-and-sidebar-panels
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - components/editor/toronto-map.tsx
autonomous: false
requirements: [SIDE-02, SIDE-03]

must_haves:
  truths:
    - "Clicking a proposal station on the map in select mode opens StationInspectorPanel in the sidebar showing name, location, and connected lines"
    - "Clicking a proposal line on the map in select mode opens LineInspectorPanel in the sidebar showing name, color, stations, and stats"
    - "Clicking empty space returns sidebar to the line list view"
    - "Inspector panels reflect updated data after station drag or auto-interchange from Plan 01"
  artifacts:
    - path: "components/editor/editor-shell.tsx"
      provides: "Sidebar panel routing for inspect-station and inspect-line"
      contains: "StationInspectorPanel"
    - path: "components/editor/toronto-map.tsx"
      provides: "Select mode click handler dispatching inspectElement"
      contains: "inspectElement"
    - path: "components/editor/sidebar/station-inspector-panel.tsx"
      provides: "Station details panel showing name, location, connected lines"
      contains: "StationInspectorPanel"
    - path: "components/editor/sidebar/line-inspector-panel.tsx"
      provides: "Line details panel showing name, color, stations, stats"
      contains: "LineInspectorPanel"
  key_links:
    - from: "components/editor/toronto-map.tsx"
      to: "lib/proposal/proposal-state.ts"
      via: "dispatch inspectElement on station/line click in select mode"
      pattern: "inspectElement.*elementType"
    - from: "components/editor/editor-shell.tsx"
      to: "components/editor/sidebar/station-inspector-panel.tsx"
      via: "sidebarPanel === inspect-station renders StationInspectorPanel"
      pattern: "inspect-station.*StationInspectorPanel"
    - from: "components/editor/editor-shell.tsx"
      to: "components/editor/sidebar/line-inspector-panel.tsx"
      via: "sidebarPanel === inspect-line renders LineInspectorPanel"
      pattern: "inspect-line.*LineInspectorPanel"
---

<objective>
Verify that clicking stations and lines on the map opens the correct sidebar inspector panels, and confirm the full Phase 9 feature set works end-to-end.

Purpose: SIDE-02 and SIDE-03 require click-to-inspect. The wiring was established in Phase 7 (select mode auto-dispatches inspectElement). This plan verifies the existing wiring works correctly with the station-first model and the Plan 01 changes (drag + auto-interchange), and includes a human verification checkpoint for the complete Phase 9 feature set.

Output: Verified click-to-inspect panels, human confirmation of all Phase 9 features.
</objective>

<execution_context>
@/Users/jonathan/dev/ttc-dreamer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/dev/ttc-dreamer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-station-drag-auto-interchange-and-sidebar-panels/09-CONTEXT.md
@.planning/phases/09-station-drag-auto-interchange-and-sidebar-panels/09-01-SUMMARY.md

<interfaces>
<!-- Key wiring already in place from Phase 7 -->

From components/editor/toronto-map.tsx (select mode click handler):
```typescript
if (activeTool === "select") {
  const features = e.features;
  if (features && features.length > 0) {
    const feature = features[0];
    const props = feature.properties as Record<string, unknown>;
    const id = props["id"] as string | null;
    if (id) {
      const elementType = feature.layer?.id === "proposal-stations-circle" ? "station" : "line";
      dispatch?.({ type: "inspectElement", payload: { id, elementType } });
    }
  }
  dispatch?.({ type: "closeInspector" }); // empty space click
}
```

From lib/proposal/proposal-state.ts (inspectElement action):
```typescript
case "inspectElement":
  return {
    ...state,
    chrome: {
      ...state.chrome,
      inspectedElementId: action.payload.id,
      sidebarPanel: action.payload.elementType === "line" ? "inspect-line" : "inspect-station",
      sidebarOpen: true,
    },
  };
```

From components/editor/editor-shell.tsx (sidebar routing):
```typescript
if (chrome.sidebarPanel === "inspect-line") {
  // Renders LineInspectorPanel with line data
}
if (chrome.sidebarPanel === "inspect-station") {
  // Renders StationInspectorPanel with station data, draft, neighbourhoods
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix click-to-inspect wiring for sidebar panels</name>
  <files>components/editor/toronto-map.tsx</files>
  <read_first>
    - components/editor/toronto-map.tsx (select mode click handler, interactiveLayerIds array)
    - components/editor/editor-shell.tsx (sidebar panel routing ~lines 270-310)
    - components/editor/sidebar/station-inspector-panel.tsx (props interface)
    - components/editor/sidebar/line-inspector-panel.tsx (props interface)
    - components/map/proposal-layers.tsx (layer IDs for proposal-stations-circle and proposal-lines-stroke)
  </read_first>
  <action>
    Verify the following end-to-end chain is correctly wired:

    1. **interactiveLayerIds** in toronto-map.tsx includes "proposal-stations-circle" and "proposal-lines-stroke" — these are the MapLibre layer IDs that make features clickable. Confirm they match the layer IDs in proposal-layers.tsx.

    2. **Select mode click handler** in toronto-map.tsx correctly:
       - Extracts `id` from feature properties
       - Determines `elementType` based on layer ID ("proposal-stations-circle" -> "station", else -> "line")
       - Dispatches `inspectElement` with `{ id, elementType }`
       - For station clicks, also sets `draggingStationId` (from Plan 01 drag feature)

    3. **Sidebar routing** in editor-shell.tsx correctly:
       - Finds the inspected line/station in `draft.lines` / `draft.stations` by `chrome.inspectedElementId`
       - Renders `LineInspectorPanel` with `line`, `draft`, `onClose` props
       - Renders `StationInspectorPanel` with `station`, `draft`, `neighbourhoods`, `onClose` props
       - Falls back to closeInspector if the element was deleted

    4. **Inspector panels** display the required data:
       - StationInspectorPanel shows: name, location (neighbourhood or coordinates), connected lines with color dots, baseline interchange link
       - LineInspectorPanel shows: name, mode label, line color stripe, length, station count, travel time, avg spacing, cost, ridership, interchange count

    If any of these checks fail, fix the issue. Common potential issues:
    - Layer IDs might have changed in a prior phase — ensure they match
    - The station click might incorrectly start a drag instead of opening the inspector (both should happen — inspect on mousedown, drag on sustained hold). The current code dispatches inspectElement AND sets draggingStationId on the same click, which means a click-and-release should open inspector + immediately clear drag. Verify this is correct: handleMouseUp clears draggingStationId, so a click (mousedown+mouseup) dispatches inspectElement AND briefly sets/clears drag state. This should be fine.

    If everything checks out and no fixes are needed, note that in a comment at the top of the verification output.
  </action>
  <verify>
    <automated>cd /Users/jonathan/dev/ttc-dreamer && npx tsc --noEmit 2>&1 | tail -10 && echo "--- Typecheck passed ---" && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "proposal-stations-circle" components/editor/toronto-map.tsx (station layer in interactiveLayerIds)
    - grep -q "proposal-lines-stroke" components/editor/toronto-map.tsx (line layer in interactiveLayerIds)
    - grep -q "inspectElement" components/editor/toronto-map.tsx (inspectElement dispatched in select mode)
    - grep -q "StationInspectorPanel" components/editor/editor-shell.tsx (station panel rendered)
    - grep -q "LineInspectorPanel" components/editor/editor-shell.tsx (line panel rendered)
  </acceptance_criteria>
  <done>
    Click-to-inspect wiring verified end-to-end: station click opens StationInspectorPanel, line click opens LineInspectorPanel, empty-space click returns to line list. TypeScript and lint pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 9 features</name>
  <files>none</files>
  <action>
    Present the human verification checklist to the user. Start the dev server if not running (`npm run dev`). Guide the user through all 6 test scenarios covering station drag, auto-interchange, and click-to-inspect sidebar panels.
  </action>
  <verify>
    <automated>echo "Human verification checkpoint — requires manual approval"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms station drag moves station and connected line geometry follows
    - User confirms auto-interchange works for proposal and baseline stations
    - User confirms clicking station opens StationInspectorPanel
    - User confirms clicking line opens LineInspectorPanel
    - User confirms clicking empty space returns to line list
  </acceptance_criteria>
  <what-built>
    Phase 9 delivers three features:
    1. Station drag — dragging a proposal station repositions it and connected line geometry follows
    2. Auto-interchange — placing a station near an existing station (proposal or baseline) automatically creates an interchange link
    3. Click-to-inspect — clicking a station or line on the map opens its details in the sidebar
  </what-built>
  <how-to-verify>
    Start the dev server: `npm run dev` and open http://localhost:3000

    **Test 1 — Station Drag (STATION-01):**
    1. Switch to draw-line mode (pencil icon in floating toolbar)
    2. Click two or more points on the map to create a line with stations
    3. Double-click to finish drawing
    4. Switch to select mode (cursor icon)
    5. Click and drag one of the stations you just placed
    6. EXPECTED: The station moves with your cursor and the line geometry follows smoothly — no visual disconnection
    7. Release the mouse button
    8. EXPECTED: The station stays at the new position and the line remains connected

    **Test 2 — Auto-Interchange with Proposal Station (DRAW-04):**
    1. Create a first line with at least 2 stations (draw-line, click, click, double-click)
    2. Start drawing a second line (click empty area to auto-create)
    3. Place a station very close to (within ~20px of) one of the first line's stations
    4. EXPECTED: Instead of a confirmation badge, the station is automatically shared between both lines — the existing station gains the second line's ID

    **Test 3 — Auto-Interchange with Baseline TTC Station (DRAW-04):**
    1. Zoom in to a TTC station (e.g., Bloor-Yonge, visible as a dot on the map)
    2. Switch to draw-line mode
    3. Click near the TTC station to start a new line
    4. EXPECTED: The new station is automatically linked to the baseline station (linkedBaselineStationId set)
    5. Click the station in select mode — EXPECTED: StationInspectorPanel shows "Connected to baseline" is not "None"

    **Test 4 — Click Station Inspector (SIDE-02):**
    1. Create a line with stations
    2. Switch to select mode
    3. Click on a station dot
    4. EXPECTED: Sidebar opens showing station name, location (neighbourhood or coordinates), and connected lines with color dots

    **Test 5 — Click Line Inspector (SIDE-03):**
    1. With a line drawn on the map
    2. Switch to select mode
    3. Click on the line path (not a station)
    4. EXPECTED: Sidebar opens showing line name, color stripe, mode type, length, station count, stats (travel time, spacing, cost, ridership)

    **Test 6 — Click Empty Space:**
    1. With an inspector panel open
    2. Click on empty map space (not on any line or station)
    3. EXPECTED: Sidebar returns to line list view
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found</resume-signal>
  <done>
    User has verified all Phase 9 features work correctly in the browser: station drag with geometry follow, auto-interchange for proposal and baseline stations, and click-to-inspect sidebar panels.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no type errors
2. `npm run lint` — no lint errors
3. Human confirms all 6 test scenarios pass in the browser
</verification>

<success_criteria>
- Clicking a proposal station opens StationInspectorPanel with name, location, connected lines
- Clicking a proposal line opens LineInspectorPanel with name, color, stations, stats
- Clicking empty space returns to line list
- Station drag moves station and connected line geometry follows
- Auto-interchange works for both proposal and baseline nearby stations
- Human approves all test scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/09-station-drag-auto-interchange-and-sidebar-panels/09-02-SUMMARY.md`
</output>
