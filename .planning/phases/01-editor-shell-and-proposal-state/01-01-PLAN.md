---
phase: 01-editor-shell-and-proposal-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/proposal/proposal-types.ts
  - lib/proposal/proposal-state.ts
  - lib/proposal/index.ts
autonomous: true
requirements: [EDTR-04, EDTR-05]
user_setup: []
must_haves:
  truths:
    - The Phase 1 shell has one typed source of truth for draft proposal state, including a baseline mode with exact values `today` and `future_committed`.
    - A default draft can be created for the Toronto sandbox shell without loading real TTC or GO data.
  artifacts:
    - lib/proposal/proposal-types.ts
    - lib/proposal/proposal-state.ts
    - lib/proposal/index.ts
  key_links:
    - lib/proposal/index.ts re-exports the proposal types and state helpers for the editor shell
    - lib/proposal/proposal-state.ts imports types from lib/proposal/proposal-types.ts and exposes the reducer/helpers the UI will consume
---

<objective>
Create the proposal and shell state foundation for Phase 1.

Purpose: Give the editor shell a future-facing draft model and a real UI-only baseline toggle state without slipping into Phase 2 data work.
Output: Typed proposal contracts plus a reducer/helper module that later shell components can wire directly into the route.
</objective>

<execution_context>
@/Users/jonathan/dev/ttc-dreamer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/dev/ttc-dreamer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-editor-shell-and-proposal-state/01-CONTEXT.md
@.planning/phases/01-editor-shell-and-proposal-state/01-RESEARCH.md
@.planning/phases/01-editor-shell-and-proposal-state/01-UI-SPEC.md
@tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define future-facing proposal and shell types</name>
  <files>lib/proposal/proposal-types.ts</files>
  <read_first>.planning/phases/01-editor-shell-and-proposal-state/01-CONTEXT.md, .planning/phases/01-editor-shell-and-proposal-state/01-RESEARCH.md, tsconfig.json</read_first>
  <action>Create `lib/proposal/proposal-types.ts` with exact exported types for the Phase 1 shell. Include `BaselineMode = "today" | "future_committed"` per D-09, a `ToolMode = "select" | "draw-line" | "add-station" | "inspect"`, and future-facing draft shapes named `ProposalDraft`, `ProposalLineDraft`, `ProposalStationDraft`, and `EditorChromeState`. `ProposalDraft` must include `baselineMode`, `title`, `lines`, and `stations`; `EditorChromeState` must include `activeTool` and `sidebarOpen`. Keep these types serializable, ASCII-only, and free of real Toronto data payloads per D-02 and D-08.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `lib/proposal/proposal-types.ts` contains `export type BaselineMode = "today" | "future_committed"`
    - `lib/proposal/proposal-types.ts` contains `export type ToolMode = "select" | "draw-line" | "add-station" | "inspect"`
    - `lib/proposal/proposal-types.ts` contains `export type ProposalDraft`
    - `lib/proposal/proposal-types.ts` does not import or embed TTC, GO, or neighbourhood data
  </acceptance_criteria>
  <done>The proposal types compile cleanly and define the exact shell contracts the UI will import.</done>
</task>

<task type="auto">
  <name>Task 2: Add initial draft state and reducer helpers</name>
  <files>lib/proposal/proposal-state.ts, lib/proposal/index.ts</files>
  <read_first>lib/proposal/proposal-types.ts, .planning/phases/01-editor-shell-and-proposal-state/01-CONTEXT.md, .planning/phases/01-editor-shell-and-proposal-state/01-RESEARCH.md</read_first>
  <action>Create `lib/proposal/proposal-state.ts` with a `createInitialProposalDraft()` helper returning a default shell draft for the Toronto sandbox, plus a reducer named `proposalEditorReducer`. The reducer must support these exact action variants only: `setBaselineMode`, `toggleSidebar`, `setActiveTool`, and `resetShellState`. The initial state must default to `baselineMode: "today"`, `activeTool: "select"`, `sidebarOpen: true`, and empty `lines` / `stations` arrays. Create `lib/proposal/index.ts` that re-exports all proposal types and state helpers. Do not add route drawing, station editing, or real data-loading actions in this phase.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `lib/proposal/proposal-state.ts` contains `export function createInitialProposalDraft()`
    - `lib/proposal/proposal-state.ts` contains `export function proposalEditorReducer`
    - `lib/proposal/proposal-state.ts` contains the strings `setBaselineMode`, `toggleSidebar`, `setActiveTool`, and `resetShellState`
    - `lib/proposal/proposal-state.ts` contains `baselineMode: "today"`
    - `lib/proposal/index.ts` re-exports from both `./proposal-types` and `./proposal-state`
  </acceptance_criteria>
  <done>The Phase 1 shell has a shared reducer/helper module ready for UI wiring, and `npm run typecheck` exits 0.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `lib/proposal/proposal-types.ts` and `lib/proposal/proposal-state.ts` export the exact baseline and tool-state contracts
- [ ] No real Toronto baseline datasets or editing actions were introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The proposal state foundation stays strictly Phase-1 scoped
  </success_criteria>

<output>
After completion, create `.planning/phases/01-editor-shell-and-proposal-state/01-01-SUMMARY.md`
</output>
