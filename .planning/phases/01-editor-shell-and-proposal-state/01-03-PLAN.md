---
phase: 01-editor-shell-and-proposal-state
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - app/page.tsx
  - components/editor/editor-shell.tsx
  - components/editor/baseline-toggle.tsx
autonomous: true
requirements: [EDTR-01, EDTR-02, EDTR-03, EDTR-04, EDTR-05]
user_setup: []
must_haves:
  truths:
    - Visiting `/` shows the full-screen editor shell instead of the bootstrap placeholder page.
    - The shell exposes a working `Today` / `Future committed` baseline toggle and a real sidebar collapse interaction backed by shared state.
    - The shell reads as a preloaded Toronto sandbox stage, but still contains no real Toronto baseline data.
  artifacts:
    - app/page.tsx
    - components/editor/editor-shell.tsx
    - components/editor/baseline-toggle.tsx
  key_links:
    - app/page.tsx imports and renders components/editor/editor-shell.tsx as the route output
    - components/editor/editor-shell.tsx imports the proposal state helpers from lib/proposal and passes state into the frame, toolbar, map stage, and sidebar
    - components/editor/baseline-toggle.tsx emits the exact labels `Today` and `Future committed` and is rendered from the toolbar shell
---

<objective>
Wire the Phase 1 shell into the route and connect it to real shell state.

Purpose: Replace the landing-page placeholder with the actual editor scaffold and make the baseline toggle/sidebar collapse behave through shared state.
Output: A client-side `EditorShell`, a dedicated `BaselineToggle` component, and a route that renders the shell by default.
</objective>

<execution_context>
@/Users/jonathan/dev/ttc-dreamer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/dev/ttc-dreamer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-editor-shell-and-proposal-state/01-CONTEXT.md
@.planning/phases/01-editor-shell-and-proposal-state/01-RESEARCH.md
@.planning/phases/01-editor-shell-and-proposal-state/01-UI-SPEC.md
@.planning/phases/01-editor-shell-and-proposal-state/01-01-PLAN.md
@.planning/phases/01-editor-shell-and-proposal-state/01-02-PLAN.md
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the interactive editor shell and baseline toggle</name>
  <files>components/editor/editor-shell.tsx, components/editor/baseline-toggle.tsx</files>
  <read_first>lib/proposal/index.ts, components/editor/editor-frame.tsx, components/editor/top-toolbar.tsx, components/editor/map-stage.tsx, components/editor/sidebar-shell.tsx, .planning/phases/01-editor-shell-and-proposal-state/01-UI-SPEC.md</read_first>
  <action>Create `components/editor/editor-shell.tsx` as a Client Component with the exact `'use client'` directive. Use `useReducer(proposalEditorReducer, undefined, createInitialProposalDraft)` from `lib/proposal/index.ts` to own shell state. Compose the frame, toolbar, map stage, and sidebar so that `activeTool`, `sidebarOpen`, and `baselineMode` come from the reducer instead of hard-coded local strings. Create `components/editor/baseline-toggle.tsx` with two controls labeled exactly `Today` and `Future committed`, using `aria-pressed` or equivalent stateful semantics and dispatching `setBaselineMode`. Keep all non-shell tools passive scaffolds; only the baseline toggle and sidebar collapse need real behavior in Phase 1.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `components/editor/editor-shell.tsx` contains `'use client'`
    - `components/editor/editor-shell.tsx` contains `useReducer(`
    - `components/editor/editor-shell.tsx` contains `createInitialProposalDraft`
    - `components/editor/baseline-toggle.tsx` contains `Today`
    - `components/editor/baseline-toggle.tsx` contains `Future committed`
    - `components/editor/baseline-toggle.tsx` contains `aria-pressed`
  </acceptance_criteria>
  <done>The shell is interactive where Phase 1 requires it, and `npm run typecheck` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the placeholder route with the Phase 1 editor shell</name>
  <files>app/page.tsx</files>
  <read_first>app/page.tsx, components/editor/editor-shell.tsx, .planning/phases/01-editor-shell-and-proposal-state/01-CONTEXT.md, .planning/phases/01-editor-shell-and-proposal-state/01-UI-SPEC.md</read_first>
  <action>Replace the current bootstrap placeholder in `app/page.tsx` with a thin route component that renders `EditorShell` immediately. Keep the route itself server-compatible and avoid adding `use client` to `app/page.tsx`. The resulting page must read like a preloaded Toronto sandbox shell rather than a generic blank canvas, but it must not import real TTC data, GO data, or context-layer files. Preserve the Phase 1 empty-state/body copy defined in the presentational shell.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `app/page.tsx` imports `EditorShell`
    - `app/page.tsx` no longer contains `Repo Bootstrap`
    - `app/page.tsx` does not contain `'use client'`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The root route renders the Phase 1 shell successfully and the production build passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm run build`
- [ ] `/` now renders the shell instead of the bootstrap placeholder
- [ ] The only real interactions are baseline mode selection and sidebar collapse
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 remains shell-only and does not introduce Phase 2 Toronto data work
  </success_criteria>

<output>
After completion, create `.planning/phases/01-editor-shell-and-proposal-state/01-03-SUMMARY.md`
</output>
