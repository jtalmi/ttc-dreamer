---
phase: 01-editor-shell-and-proposal-state
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - components/editor/editor-frame.tsx
  - components/editor/top-toolbar.tsx
  - components/editor/map-stage.tsx
  - components/editor/sidebar-shell.tsx
autonomous: true
requirements: [EDTR-01, EDTR-02, EDTR-03]
user_setup: []
must_haves:
  truths:
    - The repo has a full-screen, desktop-first editor scaffold whose largest surface is the map stage.
    - The toolbar and sidebar scaffolds exist as reusable shell components with Phase 1 placeholder behavior only.
  artifacts:
    - app/globals.css
    - components/editor/editor-frame.tsx
    - components/editor/top-toolbar.tsx
    - components/editor/map-stage.tsx
    - components/editor/sidebar-shell.tsx
  key_links:
    - components/editor/editor-frame.tsx composes the toolbar, map stage, and sidebar into one full-screen shell layout
    - components/editor/top-toolbar.tsx and components/editor/sidebar-shell.tsx expose prop-driven scaffolds that a later client shell can wire up
---

<objective>
Build the presentational shell for Phase 1.

Purpose: Establish the visible editor workspace, map-first hierarchy, and shell styling without introducing real Toronto data or editing logic.
Output: A reusable frame component, toolbar scaffold, map-stage placeholder, sidebar scaffold, and shell-level CSS tokens.
</objective>

<execution_context>
@/Users/jonathan/dev/ttc-dreamer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/dev/ttc-dreamer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-editor-shell-and-proposal-state/01-CONTEXT.md
@.planning/phases/01-editor-shell-and-proposal-state/01-RESEARCH.md
@.planning/phases/01-editor-shell-and-proposal-state/01-UI-SPEC.md
@app/globals.css
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shell-level visual tokens and background atmosphere</name>
  <files>app/globals.css</files>
  <read_first>app/globals.css, .planning/phases/01-editor-shell-and-proposal-state/01-UI-SPEC.md, .planning/phases/01-editor-shell-and-proposal-state/01-CONTEXT.md</read_first>
  <action>Extend `app/globals.css` with CSS custom properties and base selectors that match the UI-SPEC exactly: font family `IBM Plex Sans`, dominant surface `#F3EEE5`, secondary shell color `#18324A`, accent `#D85A2A`, destructive `#A83232`, and a Toronto-forward placeholder atmosphere for the map stage. Add global body/root styles that support a full-height desktop shell and avoid flat white backgrounds. Do not add any TTC or GO datasets, asset URLs, or map-library-specific selectors.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `app/globals.css` contains `--shell-dominant: #F3EEE5`
    - `app/globals.css` contains `--shell-secondary: #18324A`
    - `app/globals.css` contains `--shell-accent: #D85A2A`
    - `app/globals.css` contains a full-height selector for `body`
  </acceptance_criteria>
  <done>The shell can inherit Phase 1 visual tokens from `app/globals.css`, and lint passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create the presentational toolbar, map stage, sidebar, and frame scaffold</name>
  <files>components/editor/editor-frame.tsx, components/editor/top-toolbar.tsx, components/editor/map-stage.tsx, components/editor/sidebar-shell.tsx</files>
  <read_first>.planning/phases/01-editor-shell-and-proposal-state/01-UI-SPEC.md, .planning/phases/01-editor-shell-and-proposal-state/01-CONTEXT.md, app/page.tsx</read_first>
  <action>Create `components/editor/editor-frame.tsx` as the presentational full-screen shell wrapper. Create `components/editor/top-toolbar.tsx` with visible text-labelled scaffold controls using the exact labels `Select`, `Draw Line`, `Add Station`, and `Inspect`, plus a trailing slot for the baseline toggle. Create `components/editor/map-stage.tsx` as the dominant placeholder surface with the exact empty-state heading `Toronto draft starts here` and body copy from the UI-SPEC. Create `components/editor/sidebar-shell.tsx` as a right-hand collapsible scaffold with a 320px expanded width and 64px collapsed rail. Design these components as prop-driven shells only: they may accept `activeTool`, `collapsed`, `onToggle`, and slot props, but they must not own real editing logic or load Toronto data.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `components/editor/top-toolbar.tsx` contains the strings `Select`, `Draw Line`, `Add Station`, and `Inspect`
    - `components/editor/map-stage.tsx` contains `Toronto draft starts here`
    - `components/editor/map-stage.tsx` contains `Phase 1 sets up the shell only`
    - `components/editor/sidebar-shell.tsx` contains `320` and `64` for expanded/collapsed widths
    - `components/editor/editor-frame.tsx` composes toolbar, map, and sidebar props in one full-screen wrapper
  </acceptance_criteria>
  <done>The presentational shell exists as reusable components, and `npm run lint` exits 0.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint`
- [ ] Toolbar labels and empty-state copy match the UI-SPEC exactly
- [ ] The shell components remain placeholder-only and do not import real Toronto data
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The map stage remains the dominant surface in the shell scaffold
  </success_criteria>

<output>
After completion, create `.planning/phases/01-editor-shell-and-proposal-state/01-02-SUMMARY.md`
</output>
